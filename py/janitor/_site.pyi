from typing import Optional

def find_dist_log_failure(logf: str, length: int) -> tuple[int, tuple[int, int], Optional[list[int]]]:
    ...


def find_build_log_failure(logf: str, length: int) -> tuple[int, tuple[int, int], Optional[list[int]]]:
    ...
