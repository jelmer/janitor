syntax = "proto2";

package janitor;

message Package {
   optional string name = 1;

   optional string upstream_branch_url = 2;

   optional string branch_url = 3;
};

message OverrideConfig {
    repeated Package package = 1;
};
