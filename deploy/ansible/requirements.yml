---
collections:
  # <5.0.0: postgresql_query/postgresql_script's db: alias (used throughout
  # roles/postgres) is deprecated in favor of login_db: as of 5.0.0.
  - name: community.postgresql
    version: ">=3.0.0,<5.0.0"
